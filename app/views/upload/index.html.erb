<h1>Загрузка</h1>

<p id="notice"><%= notice %></p>

<%= form_tag({action: "upload"}, method: "post", multipart: true) do |f| %>
    <%= file_field_tag 'picture' %>
    <%= submit_tag("upload") %>
<% end %>